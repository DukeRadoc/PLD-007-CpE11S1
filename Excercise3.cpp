#include <iostream>
using namespace std;

int main()
{
		int x=0, y=0;
		
		cout<<"Input X value :";
		cin>>x ;
		cout<<"Input Y value :";
		cin>>y ;
		
		if(x>y)
		{cout<<"X is greater than Y";}
		else if(x<y)
		{cout<<"Y is greater than X";}
		else
		{cout<<"X and Y are equals";}

	return 0;
}
